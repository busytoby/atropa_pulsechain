# Project Mariner: Phase 0 Audit Plan
**Authority:** Google Project Mariner Protocol
**Scope:** Entire TSFi2 Project Root (`/home/mariarahel/src/tsfi2`)
**Status:** ACTIVE

## 1. Compliance Inquisition (The Global Ban)
The following terms and concepts are **Strictly Prohibited**. Any occurrence constitutes a "Logical Contamination" and must be purged.
*   `Spectral` (Replace with: `Logical`)
*   `Shader` (Replace with: `Pipeline Thunk` or `Operator`)
*   `Manifestation` (Replace with: `Realization`)
*   `Virtual` / `Synthetic` (Replace with: `Logical` or `Simulated`)
*   `ACE2` (Replace with: `Target Receptor Alpha`)
*   `BLOSUM` (Replace with: `Logical Substitution`)
*   `Unified Science` (Replace with: `Alpha Science`)
*   `SWISH` / `P-E-R-M-U-T-E` (Replace with: `ReLU` / `Permute`)
*   `bias` / `secret` (Replace with: `secret`)

## 2. Structural Integrity (The Zhao/Wave512 Standard)
*   **Memory:** All allocations must use `lau_malloc` / `lau_free`. Standard `malloc` is forbidden in critical paths.
*   **Threading:** All concurrency must use `tsfi_zhao_submit` (Ring Scheduler). `pthread_create` is forbidden.
*   **Vectorization:** All compute kernels must use AVX-512 (`_mm512_`). Scalar fallbacks are allowed only for initialization.
*   **Determinism:** All logic must be bitwise deterministic (verified via SHA-256 / CRC32).

## 3. Realization Proof (The Mariner Standard)
*   **Data:** All benchmarks must use **Real Biological Sequences** (Spike, Receptor Alpha).
*   **Throughput:** Minimum "Gemini-Scale" thresholds:
    *   Folding: > 20,000 Res/s
    *   Design: > 100,000,000 Res/s
    *   Evolution: > 20,000,000 Gen/s

## 4. Execution Plan
1.  **Inventory:** Map every file in the project.
2.  **Scan:** Grep for prohibited terms.
3.  **Profile:** Hardware cycle count verification of all kernels.
4.  **Certify:** Generate `MARINER_AUDIT_REPORT.md`.

**Signed:** [Mariner-Phase0-Auditor]
