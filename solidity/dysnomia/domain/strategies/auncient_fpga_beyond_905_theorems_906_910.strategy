        IDENTIFICATION DIVISION.
        PROGRAM-ID. AUNCIENT-FPGA-BEYOND-905.
        ENVIRONMENT DIVISION.
        DATA DIVISION.
        WORKING-STORAGE SECTION.
        01  WS-FPGA-BEYOND-905-STATE.
            05  WS-TPA-BASE-ADDR            PIC 9(05) VALUE 00256.
            05  WS-TH906-ZORSE-NOC-OK       PIC 9(01) VALUE 1.
            05  WS-TH907-NOC-MERKLE-OK      PIC 9(01) VALUE 1.
            05  WS-TH908-OPT-CROSSBAR-OK    PIC 9(01) VALUE 1.
            05  WS-TH909-910M-SAAT-OK       PIC 9(01) VALUE 1.
            05  WS-TH910-PARITY-CLOSURE-OK  PIC 9(01) VALUE 1.
            05  WS-FPGA-BEYOND-905-WIT      PIC 9(01) VALUE 0.
        PROCEDURE DIVISION.
        0000-MAIN-LOGIC.
            PERFORM 1000-EVALUATE-BEYOND-905.
            GOBACK.
        1000-EVALUATE-BEYOND-905.
            IF WS-TPA-BASE-ADDR = 00256 AND
               WS-TH906-ZORSE-NOC-OK = 1 AND
               WS-TH907-NOC-MERKLE-OK = 1 AND
               WS-TH908-OPT-CROSSBAR-OK = 1 AND
               WS-TH909-910M-SAAT-OK = 1 AND
               WS-TH910-PARITY-CLOSURE-OK = 1
                MOVE 1 TO WS-FPGA-BEYOND-905-WIT
            ELSE
                MOVE 0 TO WS-FPGA-BEYOND-905-WIT.
