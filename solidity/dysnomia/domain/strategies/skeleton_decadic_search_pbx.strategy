       IDENTIFICATION DIVISION.
       PROGRAM-ID. SKELETON-DECADIC-SEARCH-STRATEGY.
       AUTHOR. AUNCIENT DYSNOMIA VM / DECADIC PBX SEARCH AUDIT.

       ENVIRONMENT DIVISION.
       CONFIGURATION SECTION.

       DATA DIVISION.
       WORKING-STORAGE SECTION.
       01 WS-DECADIC-PARAMETERS.
          05 WS-DIGIT-1                PIC 9(01) VALUE 5.
          05 WS-DIGIT-2                PIC 9(01) VALUE 5.
          05 WS-DIGIT-3                PIC 9(01) VALUE 7.
          05 WS-DIGIT-4                PIC 9(01) VALUE 0.
          05 WS-CICS-WRITER-ID         PIC 9(04) VALUE 555.
          05 WS-PLL-PHASE-ERR          PIC S9(05) VALUE +00064.
          05 WS-K-EXPONENT             PIC 9(01) VALUE 3.
          05 WS-SIMULATE-ABORT         PIC X(01) VALUE 'N'.

       01 WS-INTERMEDIATE-REGISTERS.
          05 WS-NODE-INDEX             PIC 9(05) VALUE 0.
          05 WS-SHADOW-BASELINE        PIC 9(12) VALUE 0.
          05 WS-G-GATE-FACTOR          PIC 9(04) VALUE 0.
          05 WS-COMMITTED-OUTPUT       PIC 9(12) VALUE 0.
          05 WS-DISP-WRAP-MOD          PIC 9(03) VALUE 0.
          05 WS-ABS-PHASE-ERR          PIC 9(05) VALUE 0.

       01 WS-CLASSIFICATION-RULINGS.
          05 WS-RULING-CODE            PIC 9(02) VALUE 0.
             88 QUALIFIED-ORBITAL-HANDSHAKE VALUE 0.
             88 INVALID-K-EXPONENT          VALUE 1.
             88 INVALID-DIAL-DIGIT          VALUE 2.
             88 CICS-CITIZEN-UNVERIFIED     VALUE 3.
             88 PLL-PHASE-LOCK-FAIL         VALUE 4.
             88 GATING-CLAMP-OVERFLOW       VALUE 5.
             88 SHADOW-ISOLATION-CORRUPT    VALUE 6.
             88 CASCADE-ROLLBACK-FAIL       VALUE 7.

       PROCEDURE DIVISION.
       0000-MAIN-EVALUATION.
           PERFORM 1000-VALIDATE-PREREQUISITES.
           IF WS-RULING-CODE EQUAL ZERO
               PERFORM 2000-EXECUTE-DECADIC-SEARCH
               PERFORM 3000-VERIFY-ACID-INVARIANTS
           END-IF.
           STOP RUN.

       1000-VALIDATE-PREREQUISITES.
           IF WS-K-EXPONENT NOT EQUAL 3
               SET INVALID-K-EXPONENT TO TRUE
               EXIT PARAGRAPH
           END-IF.

           IF WS-DIGIT-1 > 9 OR WS-DIGIT-2 > 9 OR
              WS-DIGIT-3 > 9 OR WS-DIGIT-4 > 9
               SET INVALID-DIAL-DIGIT TO TRUE
               EXIT PARAGRAPH
           END-IF.

           IF WS-CICS-WRITER-ID NOT EQUAL 555 AND
              WS-CICS-WRITER-ID NOT EQUAL 888 AND
              WS-CICS-WRITER-ID NOT EQUAL 99  AND
              WS-CICS-WRITER-ID NOT EQUAL 42
               SET CICS-CITIZEN-UNVERIFIED TO TRUE
               EXIT PARAGRAPH
           END-IF.

           IF WS-PLL-PHASE-ERR < -512 OR WS-PLL-PHASE-ERR > +512
               SET PLL-PHASE-LOCK-FAIL TO TRUE
               EXIT PARAGRAPH
           END-IF.

       2000-EXECUTE-DECADIC-SEARCH.
           COMPUTE WS-NODE-INDEX = (WS-DIGIT-1 * 1000) + (WS-DIGIT-2 * 100) + (WS-DIGIT-3 * 10) + WS-DIGIT-4.
           COMPUTE WS-SHADOW-BASELINE = (WS-CICS-WRITER-ID * 10000) + WS-NODE-INDEX.

           COMPUTE WS-G-GATE-FACTOR = 875 + ((125 * (WS-DIGIT-1 + 1)) / 10).
           IF WS-G-GATE-FACTOR < 875 OR WS-G-GATE-FACTOR > 1000
               SET GATING-CLAMP-OVERFLOW TO TRUE
               EXIT PARAGRAPH
           END-IF.

           IF WS-PLL-PHASE-ERR < 0
               COMPUTE WS-ABS-PHASE-ERR = - WS-PLL-PHASE-ERR
           ELSE
               MOVE WS-PLL-PHASE-ERR TO WS-ABS-PHASE-ERR
           END-IF.

           IF WS-SIMULATE-ABORT EQUAL 'Y'
               MOVE WS-SHADOW-BASELINE TO WS-COMMITTED-OUTPUT
           ELSE
               COMPUTE WS-COMMITTED-OUTPUT = (((WS-SHADOW-BASELINE + ((512 - WS-ABS-PHASE-ERR) * 16)) * WS-G-GATE-FACTOR) / 1000)
           END-IF.

           COMPUTE WS-DISP-WRAP-MOD = FUNCTION MOD(WS-COMMITTED-OUTPUT, 256).

       3000-VERIFY-ACID-INVARIANTS.
           IF WS-SHADOW-BASELINE NOT EQUAL ((WS-CICS-WRITER-ID * 10000) + WS-NODE-INDEX)
               SET SHADOW-ISOLATION-CORRUPT TO TRUE
               EXIT PARAGRAPH
           END-IF.

           IF WS-SIMULATE-ABORT EQUAL 'Y' AND WS-COMMITTED-OUTPUT NOT EQUAL WS-SHADOW-BASELINE
               SET CASCADE-ROLLBACK-FAIL TO TRUE
               EXIT PARAGRAPH
           END-IF.

           SET QUALIFIED-ORBITAL-HANDSHAKE TO TRUE.
