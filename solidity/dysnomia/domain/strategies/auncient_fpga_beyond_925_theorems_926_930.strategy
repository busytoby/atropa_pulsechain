        IDENTIFICATION DIVISION.
        PROGRAM-ID. AUNCIENT-FPGA-BEYOND-925.
        ENVIRONMENT DIVISION.
        DATA DIVISION.
        WORKING-STORAGE SECTION.
        01  WS-FPGA-BEYOND-925-STATE.
            05  WS-TPA-BASE-ADDR            PIC 9(05) VALUE 00256.
            05  WS-TH926-MIMETIC-OK         PIC 9(01) VALUE 1.
            05  WS-TH927-WEIGHT-MERKLE-OK   PIC 9(01) VALUE 1.
            05  WS-TH928-QUERY-LAT-OK       PIC 9(01) VALUE 1.
            05  WS-TH929-930M-SAAT-OK       PIC 9(01) VALUE 1.
            05  WS-TH930-PARITY-CLOSURE-OK  PIC 9(01) VALUE 1.
            05  WS-FPGA-BEYOND-925-WIT      PIC 9(01) VALUE 0.
        PROCEDURE DIVISION.
        0000-MAIN-LOGIC.
            PERFORM 1000-EVALUATE-BEYOND-925.
            GOBACK.
        1000-EVALUATE-BEYOND-925.
            IF WS-TPA-BASE-ADDR = 00256 AND
               WS-TH926-MIMETIC-OK = 1 AND
               WS-TH927-WEIGHT-MERKLE-OK = 1 AND
               WS-TH928-QUERY-LAT-OK = 1 AND
               WS-TH929-930M-SAAT-OK = 1 AND
               WS-TH930-PARITY-CLOSURE-OK = 1
                MOVE 1 TO WS-FPGA-BEYOND-925-WIT
            ELSE
                MOVE 0 TO WS-FPGA-BEYOND-925-WIT.
