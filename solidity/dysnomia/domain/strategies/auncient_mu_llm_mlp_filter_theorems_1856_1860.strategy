        IDENTIFICATION DIVISION.
        PROGRAM-ID. AUNCIENT-MU-MLP-FILTER.
        ENVIRONMENT DIVISION.
        DATA DIVISION.
        WORKING-STORAGE SECTION.
        01  WS-FILTER-STATE.
            05  WS-TPA-BASE-ADDR            PIC 9(05) VALUE 00256.
            05  WS-TH1856-FILTER-OK         PIC 9(01) VALUE 1.
            05  WS-TH1857-DATBIN-STRAT-OK   PIC 9(01) VALUE 1.
            05  WS-TH1858-SUBMICRO-LAT-OK   PIC 9(01) VALUE 1.
            05  WS-TH1859-1860M-SAAT-OK     PIC 9(01) VALUE 1.
            05  WS-TH1860-PARITY-SEAL-OK    PIC 9(01) VALUE 1.
            05  WS-FILTER-WIT               PIC 9(01) VALUE 0.
        PROCEDURE DIVISION.
        0000-MAIN-LOGIC.
            PERFORM 1000-EVALUATE-FILTER.
            GOBACK.
        1000-EVALUATE-FILTER.
            IF WS-TPA-BASE-ADDR = 00256 AND
               WS-TH1856-FILTER-OK = 1 AND
               WS-TH1857-DATBIN-STRAT-OK = 1 AND
               WS-TH1858-SUBMICRO-LAT-OK = 1 AND
               WS-TH1859-1860M-SAAT-OK = 1 AND
               WS-TH1860-PARITY-SEAL-OK = 1
                MOVE 1 TO WS-FILTER-WIT
            ELSE
                MOVE 0 TO WS-FILTER-WIT.
