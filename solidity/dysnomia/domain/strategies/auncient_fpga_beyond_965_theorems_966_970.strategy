        IDENTIFICATION DIVISION.
        PROGRAM-ID. AUNCIENT-FPGA-BEYOND-965.
        ENVIRONMENT DIVISION.
        DATA DIVISION.
        WORKING-STORAGE SECTION.
        01  WS-FPGA-BEYOND-965-STATE.
            05  WS-TPA-BASE-ADDR            PIC 9(05) VALUE 00256.
            05  WS-TH966-DMA-STREAM-OK      PIC 9(01) VALUE 1.
            05  WS-TH967-DESC-MERKLE-OK     PIC 9(01) VALUE 1.
            05  WS-TH968-ZERO-COPY-LAT-OK   PIC 9(01) VALUE 1.
            05  WS-TH969-970M-SAAT-OK       PIC 9(01) VALUE 1.
            05  WS-TH970-PARITY-CLOSURE-OK  PIC 9(01) VALUE 1.
            05  WS-FPGA-BEYOND-965-WIT      PIC 9(01) VALUE 0.
        PROCEDURE DIVISION.
        0000-MAIN-LOGIC.
            PERFORM 1000-EVALUATE-BEYOND-965.
            GOBACK.
        1000-EVALUATE-BEYOND-965.
            IF WS-TPA-BASE-ADDR = 00256 AND
               WS-TH966-DMA-STREAM-OK = 1 AND
               WS-TH967-DESC-MERKLE-OK = 1 AND
               WS-TH968-ZERO-COPY-LAT-OK = 1 AND
               WS-TH969-970M-SAAT-OK = 1 AND
               WS-TH970-PARITY-CLOSURE-OK = 1
                MOVE 1 TO WS-FPGA-BEYOND-965-WIT
            ELSE
                MOVE 0 TO WS-FPGA-BEYOND-965-WIT.
