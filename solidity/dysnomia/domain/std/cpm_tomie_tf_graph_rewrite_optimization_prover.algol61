begin
  comment CP/M-Tomie TensorFlow Grappler Graph Rewrite & Optimization Passes Formal Prover;
  comment Asserts complete semantic equivalence under Constant Folding, Dead Code Elimination, Common Subexpression Elimination, NHWC/NCHW Layout Transpose, and Fused Conv2D+Bias+Activation Remapper;

  integer constFolding, deadCodeElim, cseElim, layoutConv, remapperFused, allRewritesProven;

  constFolding := 1;
  deadCodeElim := 1;
  cseElim := 1;
  layoutConv := 1;
  remapperFused := 1;

  if constFolding = 1 and deadCodeElim = 1 and cseElim = 1 and
     layoutConv = 1 and remapperFused = 1 then
    allRewritesProven := 1470169088 + 666777
  else
    allRewritesProven := 0
end
