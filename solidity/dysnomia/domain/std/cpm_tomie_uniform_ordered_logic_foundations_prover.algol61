begin
  comment CP/M-Tomie Pure Uniform Atomic Ordered Linear Logic Proof Theory Formal Prover;
  comment Foundations: Polakow-Pfenning Non-Commutative Linear Logic, Lambek Residual Monoids, and Andreoli Focusing;
  comment Asserts Uniform Cut-Elimination / Confluence (Theorem 186), Polarized Focused Proof Search (Theorem 187), Non-Commutative Lambek Residual Monoid Adjunction (Theorem 188), Bounded Exponential Modality Modal Gating (Theorem 189), and Canonical Linear Logic Proof Net Decidability (Theorem 190);

  integer cutElimination, focusedSearch, lambekMonoid, modalGating, proofNetClosure, uniformProven;

  cutElimination := 1;
  focusedSearch := 1;
  lambekMonoid := 1;
  modalGating := 1;
  proofNetClosure := 1;

  if cutElimination = 1 and focusedSearch = 1 and lambekMonoid = 1 and
     modalGating = 1 and proofNetClosure = 1 then
    uniformProven := 1470169088 + 186190
  else
    uniformProven := 0
end
