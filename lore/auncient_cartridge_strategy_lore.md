# Auncient Cartridge Strategy Lore

The integration of the Dysnomia VM and the unified visual compositor represents a milestone in Auncient Wavelet storage paradigms. By combining 6502 ROM emulation with modern dynamic memory-mapped register modifications, the system enables real-time vector coordination overlays directly atop low-level firmware.

## Dynamic NPC Strategies
In auncient hardware routing structures, NPC behavior rules must remain unified to fit within the small-binary constraints of physical cartridges:
1. **Adversarial Hunter Droid (`s1`)**: Uses proximity-weighted vector seeking behavior, targeting both active player ships and helper beacons to disrupt keycode transmission patterns.
2. **Orbital Mine (`s2`)**: Executed via a constant trigonometric orbit loop, utilizing a cosine radial pulse representing physical field-effect transistor (FET) discharge cycles.
3. **Helper Droid (`s3`)**: An autonomous interop unit programmed to shadow player coordinates, maintaining a target distance while balancing shield dynamics and injecting WinchesterMQ keycode structures.

## Synthesizer Interface Controllers
Modern C64 cartridge interfaces allow external voice registers to act as input telemetry. Sound generation frequencies feed back into the memory mapper:
- Low-pitch waves translate to horizontal push impulses.
- High-pitch waves trigger register overrides to toggle shields and execute action triggers, linking sound synthesis and low-level state machines.
